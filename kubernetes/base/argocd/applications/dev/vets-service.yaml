apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: petclinic-dev-vets-service
  namespace: argocd
  labels:
    project: petclinic
    environment: dev
    service: vets-service
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: petclinic
  source:
    repoURL: https://github.com/petclinic-project/petclinic-platform.git
    targetRevision: HEAD
    path: petclinic-eks-deployment/helm/petclinic-service
    helm:
      releaseName: vets-service
      valueFiles:
        - ../../helm-values/vets-service.yaml
        - ../../helm-values/dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: petclinic-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
